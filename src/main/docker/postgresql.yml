name: hayetfertility
services:
  postgresql:
    image: postgres:17.0
    environment:
      - POSTGRES_USER=hayetFertility
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_HOST_AUTH_METHOD=trust
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U $${POSTGRES_USER}']
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - 127.0.0.1:5432:5432
    volumes:
      - ~/volumes/jhipster/hayetFertility/postgresql/:/var/lib/postgresql/data/
